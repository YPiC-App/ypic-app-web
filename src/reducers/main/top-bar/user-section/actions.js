import payloadAction from 'payload-action-creator';

export const SET_MENU_ANCHOR_ELEMENT = '@MAIN/@TOP_BAR/SET_MENU_ANCHOR_ELEMENT';

export const setMenuAnchorElement = payloadAction(SET_MENU_ANCHOR_ELEMENT);
